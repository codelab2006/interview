// The MutationObserver interface provides the ability to watch for changes being made to the DOM tree;
MutationObserver;

// The Intersection Observer API provides a way to asynchronously observe changes in the intersection of
// a target element with an ancestor element or with a top-level document's viewport.
IntersectionObserver;

// You can get additional control over animations — as well as useful information about them — by making
// use of animation events
AnimationEvents;

// The Promise object represents the eventual completion (or failure) of an asynchronous operation,
// and its resulting value
// new Promise( /* executor */ function(resolve, reject) { ... } );
// A function that is passed with the arguments resolve and reject. The executor function is
// executed immediately by the Promise implementation, passing resolve and reject functions
// (the executor is called before the Promise constructor even returns the created object).
Promise;
